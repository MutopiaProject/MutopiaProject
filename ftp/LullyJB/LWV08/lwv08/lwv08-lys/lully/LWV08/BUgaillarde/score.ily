
\score {
  \dessusHauteContreTailleQuinteBasse
  \header {
    titre = "Gaillarde pour les parents et amis des mariés."
    breakbefore = #(break-before?)
  }
  \layout { #(define tweak-key (*current-piece*)) }
  \midi { \tempo 4 = 160 }
}
